{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main local app window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
